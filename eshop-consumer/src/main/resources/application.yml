server:
  port: 8083
spring:
  application:
    name: eureka-consumer
    
eureka:

  client:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8080/eureka
  
feign:
  hystrix:
     enabled:true    

